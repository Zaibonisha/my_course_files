// This is a simple JavaScript file that will be linked to the HTML file

// Declare a variable
let number = 3;

// Loop to print numbers from 1 to 5 in the console
for (let i = 1; i <= 5; i++) {
    console.log(i);
}

// Set a condition and log the result
if (number > 2) {
    console.log('The number is greater than 2');
} else {
    console.log('The number is 2 or less');
}
